<?php

class FramesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('frames')->truncate();

		$frames = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('frames')->insert($frames);
	}

}
